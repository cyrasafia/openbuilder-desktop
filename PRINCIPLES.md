# 设计原则

## 原则：保持精简（Keep Lean）

设计的核心不是关于「做什么」，而是关于「不做什么」

保持精简这一原则应该是不言自明的，又可以被阐释为以下三条具体原则

### 原则一：服务用户

为了保持精简，必须研究目标用户、以及用户的实际工作流。

只有真正用户需要、对效率有帮助的功能才做；非目标用户的、空想的需求不做。

### 原则二：不做大而全

不做「什么都有、什么都不精」的产品。

已经有成熟软件做的非常好的功能不做，比如代码编辑。应该在必要的时候让工作流丝滑地迁移到外部软件。

使用频率高、外部软件做不好的功能应该做，比如 Markdown 预览，使用频度高，且外部软件普遍缺 TOC 这一核心功能。

在项目内实现有独特优势的功能应该做，比如 Terminal ，在内部实现可以继承项目路径，减少一次 cd 切换。

### 原则三：理念先行

从需求直接推导功能容易导致形散。

因为满足同一个需求，有不同的实现路径，缺少理念就无法做取舍。

因此必须先从需求推出设计理念，再根据理念对功能做取舍。

## 理念：Everything is a tab

本项目的设计理念是「Everything is a tab」，这一理念是由目标用户的需求推导出来的。

### 目标用户

**目标用户是所有 builder，不止程序员**——用 agent 建造东西的人。builder 的工作方式：

1. **agent 工作，人监督和验收**——介入闭环是「看 → 判断 → 说」，不是亲手改
2. **多任务**——同时盯着多个项目/工作区里的多个 agent
3. **工作内容不仅仅是编码**——文档、网页、数据分析都在产出范围内

### 产品定位

**builder 与 AI 结对的桌面驾驶舱：以作用域为纲、会话为主体，把「驱动—观察—介入」三类界面组织在单一框架里。**

- **驱动**（向 agent 输入）：输入框、@ 引用、附件、斜杠命令、agent/model 切换
- **观察**（agent 在做什么）：消息流、diff、文件树、任务列表、子代理状态、markdown/图片/PDF/HTML 预览
- **介入**（把关/亲自下场）：授权卡、问题卡、补充发送、终端、浏览器

工作流循环：选定作用域 → 驱动 → 观察 → 介入 → 归档。

**与移动端分工**：openbuilder（Flutter）是「在路上盯着」（碎片时间、单栏、降级观察），桌面端是「在工位上盯着并随时介入」（常驻、多栏、全量介入面）。同一 server 的两个视图，按场景分工。

### 功能取向

**1（监督验收）→ 主要做浏览，不做/少做编辑。**

完成工作的是 Agent，人主要做验收，因此对人来说「读」比「写」更重要。

Agent 是多线程工作，但人的注意力是单线程，因此读的功能应该保持注意力聚焦。

**2（多任务）→ 多项目切换、worktree、状态指示是核心。**

任务的载体是目录、而不是会话。因为同一个目录内多个 agent 执行写操作很容易冲突，一个项目内多任务的最佳实践是开多个 worktree，worktree 的创建、删除必须非常简单。少数情况下需要同一个 worktree 内多个 session 同时工作，应该用指示器解决。

多任务并行时，每个任务的状态非常重要，因此指示器必须有非常强的视觉指示。

**3（非纯编码）→ 代码并不优先，各种文件格式平权。**

代码、diff 不应该比其他格式更优先考虑，markdown、图片、网页、pdf、二进制文件也同等重要。

### 界面结构

工作流「**人指挥多 agent 并行工作并验收成果**」推出三栏布局：

- **左栏**（项目 + worktree）= 多任务管理，每项代表一个 agent
- **右栏**（文件树）= agent 产出物的目录
- **中栏**（工作区）= 人与 agent 沟通、检验成果的空间

任务的载体是目录 → 左栏展示以 worktree 为粒度，配合状态指示器

人的注意力是单线程的 → 工作区不应该再分屏，应该用 tab 切换

文件格式平权 → 会话、文件、terminal、diff、网页都是 tab 形式



以上理念以一句话概括就是：「Everything is a tab」
