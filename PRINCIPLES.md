# 设计原则

> 本文是产品定位与设计原则的唯一权威落点（2026-09-04 由 v0.4 规划前的原则讨论蒸馏成文）。spec-*.md 的范围取舍、design-*.md 的方案设计，以及一切「做不做 / 怎么做 / 放哪里」的争论，以本文为判据源；原则本身的修订 = 改写本文并注明日期与依据（决策记录性质，同 `docs/design-architecture.md`）。
>
> 层次关系：**元原则「保持精简」（§3）**贯穿一切，展开为**三条核心原则（§2）**——服务用户 / 不做大而全 / 理念先行；原则运用于产品得到**产品层理念**（§1 驾驶舱），运用于界面得到**界面层理念**（§2.3 Everything is a tab）。

## 1. 产品定位：builder 的桌面驾驶舱

**目标用户是所有 builder，不止程序员**——用 agent 建造东西的人。builder 的工作方式：

1. **agent 工作，人监督和验收**——介入闭环是「看 → 判断 → 说」，不是亲手改
2. **多任务**——同时盯着多个项目/工作区里的多个 agent
3. **工作内容不仅仅是编码**——文档、网页、数据分析都在产出范围内

由此推出本项目是什么：

> **builder 与 AI 结对的桌面驾驶舱：以作用域为纲、会话为主体，把「驱动—观察—介入」三类界面组织在单一框架里。**

- **驱动**（向 agent 输入）：输入框、@ 引用、附件、斜杠命令、agent/model 切换
- **观察**（agent 在做什么）：消息流、diff、文件树、任务列表、子代理状态、markdown/图片/PDF/HTML 预览
- **介入**（把关/亲自下场）：授权卡、问题卡、补充发送、终端、浏览器

工作流循环：选定作用域 → 驱动 → 观察 → 介入 → 归档。

**检验闭环的对称性**（1×3 的交叉推导）：产出分两类，检验面两个家族——文档/网页类产出 → **预览家族**；代码类产出 → **终端家族**（build/调试/抓日志/git）。两个家族都因「零切换 + 带上下文」而嵌入，缺任一半，验收环节都要往外跳。

**与移动端分工**：openbuilder（Flutter）是「在路上盯着」（碎片时间、单栏、降级观察），桌面端是「在工位上盯着并随时介入」（常驻、多栏、全量介入面）。同一 server 的两个视图，按场景分工。

## 2. 三条核心原则

### 2.1 原则一：服务用户——从 builder 工作流出发设计

功能的价值判据只有工作流。三条工作流各自的推导：

**1（监督验收）→ 主要做浏览，不做/少做编辑。**
介入通道是对话（告诉 agent 改），手改文件破坏「agent 干活、人把关」的心智——所以代码编辑不做是**理念性排除**，不只是省重量。验收的主载体是 diff 而非文件（会话 diff / 上一轮 diff 已落地，message revert 是「验收不合格」的回路）；「快速看懂改动」优先于「精读文件」——diff 是第一公民，code-view 是第二。

**2（多任务）→ 多项目切换、worktree、状态指示是核心。**
基础层已完成：作用域指示器（waiting > error > running 分色计数）、Ctrl+Alt+↑/↓ 循环切换、worktree 二级管理。二阶缺口——多任务监督的真问题是「哪个会话在我没看时有了新输出/在等我」，waiting 只覆盖「在等我」，缺**未读/新活动标记**。多任务 ≠ 分屏/多窗口：注意力是单线程的，解法是分层（见 §2.3）。

**3（非纯编码）→ 预览家族是一等公民。**
markdown（TOC/front matter/mermaid）、图片、PDF、HTML 均已落地。两条推论：**附件上传**是它的输入侧（产出多样 → 喂给 agent 的材料也多样：截图/设计稿/文档）；**managed 模式**（含二进制安装）是它的前置（非程序员不会用 npm/CLI 装 server）。缺口：**预览的实时性**——监督的是 agent 反复迭代，预览应随文件变更刷新（浏览器 Tab 手动刷新、PDF 重开才可见）。

### 2.2 原则二：不做大而全——做、租、移交

只做简单的、高频的、重要的功能；不做过于复杂的、其他软件已经做得很好的功能；必要时**丝滑地把工作流移交给外部软件**。

判据是两个独立维度的矩阵——「别人做得好不好」（能力）×「离开的代价」（交接成本）：

| | 别人做得好 | 别人做不好 / 做不了 |
|---|---|---|
| **离开成本低** | 不做：代码编辑（IDE）、浏览器深度功能（收藏/地址栏搜索/密码管理）、markdown 所见即所得 | 做深：markdown 浏览（外部普遍缺 TOC/front matter） |
| **离开成本高** | 最小嵌入：终端、浏览器 Tab（只承担预览产出） | 护城河：见下 |

**做深的完整条件**：原则一判定重要 ×（别人做不好 ∨ 只有我们有上下文 ∨ 离开会打断工作流）。

**「别人做不好」分两档，最高档是护城河**：

- **能力档**：别人有能力缺口——如 markdown 浏览普遍缺 TOC/front matter。我们真正赢的是**工作流位置**：零打开成本、作用域上下文、随文件监听刷新
- **数据档：外部工具结构上做不了**——「上一轮 diff」（哪个改动属于哪轮 agent 工作，IDE/lazygit 没有会话语义）、任务列表、子代理状态、waiting 指示。凡**依赖会话/作用域元数据的观察面**都是独占领地。这是功能评估的最高优先级信号：数据在我们手里的观察面优先做

**终端（示范案例）**：对标终端模拟器（Alacritty），不是 tmux。它重要的原因是**结果检验**（开发项目要 build/调试/抓日志/git 操作）；外部做不好的原因是**缺上下文**（默认打开在 home、不知道项目、无从拖放）。拆三层，只做中间层是我们的：

| 层 | 归属 | 理由 |
|---|---|---|
| 仿真（渲染/转义序列） | xterm.js | 租赁：别人做得好 |
| 复用（分屏/持久会话） | tmux 领地 | 明确不做 |
| **上下文集成**（cwd=作用域、Tab 生命周期、cursor 回放、断线重连） | 我们 | 别人没有作用域概念，结构上做不了 |

> **终端是 CLI 生态的租赁接口：一次集成，租下整个生态（git/build/日志/curl），且不与用户的肌肉记忆竞争。** 对照 openchamber 的图形化 git——自建平行物，成本高、永远落后于 git 本身、还要教新交互。本项目明确不走该路线（与参考项目的分歧点，2026-09-04）。租 PDFium 不自研 PDF 渲染、租 Chromium 不自研浏览器内核，同一决策模式：**租赁成熟生态，不自建平行物**。

**移交机制**：丝滑 = **带着上下文离开**，不让用户重新定位。已有：打开 / 打开方式（带 MIME）/ 引用到会话 / 复制路径（文件树右键），外链自动走系统浏览器。缺口：右键「在文件管理器中显示」（reveal + 选中）、浏览器工具栏「用系统浏览器打开当前页」显式出口。

### 2.3 原则三：理念先行——需求 → 理念 → 功能

先由用户需求推导设计理念，再根据理念选择功能，而不是功能清单的随手叠加。理念必须**双向成立**：能事后解释全部既有结构（否则是错的），也能裁决未来（否则是装饰）。

推导链（本项目的主示范）——工作流「**人指挥多 agent 并行工作并验收成果**」推出三栏布局：

- **左栏**（项目 + worktree）= **工位**：工作的 agent 所在之处
- **右栏**（文件树）= agent 产出的 **index**
- **中栏**（工作区）= 人与 agent 沟通、检验成果的**主战场**

**以 worktree 而非 session 作为左栏的 agent 代表**：写操作下两个 session 挤一个工作区必然互扰，正确姿势是两个 worktree 各一个 session。精确表述：**worktree 是并行度的单位（空间隔离），session 是对话的单位（时间序列）**——左栏导航到工位，坐在工位上的工人才是 session，指示器显示工位上工人的状态；偶发一工位多 session，用指示器区分可见性、接受残余互扰风险。global 项目按目录拆分（键 `global\0<directory>`）复用同一逻辑：目录即隔离单位。

**工作区内 tab 而非分屏**：注意力是单线程的，不会一边看代码、一边看文档、一边和 agent 说话；单一工作区既保持注意力集中，又给重活（diff/预览）留足展示空间。多任务与单线程注意力的矛盾**用分层解决**：

- 周边视觉（不占注意力）：左栏指示器、状态计数、（未来）未读标记
- 焦点切换（占注意力）：tab

分屏/多窗口由此被**双理由**否决：注意力单线程（用户事实）+ EiaT（结构不成立）——两个理由互相独立、结论一致，是最硬的排除。

### 界面理念：Everything is a tab

**工作区的一切皆 tab；非工作区内容不得变成 tab**（导航进左栏、状态进指示器、设置进弹窗、授权/问题进卡片）。它同时是排除器（否决分屏、浮窗、画中画、独立窗口）和准入表——任何新视图要进中栏，必须回答三问：

1. kind 与稳定标识是什么？（注册制：chat=sessionID、file=路径、diff=目录、terminal=ptyID、browser=URL）
2. 打开/关闭语义是什么？（chat 关闭=归档；terminal 关闭=杀 pty；browser 关闭=dispose view）
3. 是否要求同时可见两块内容（分屏/浮窗/第二窗口）？→ 违反即否决

既有五类 tab（chat/file/diff/terminal/browser）全部合规。未来如做「会话累计改动总览」，形态已被本文规定：一个 tab kind，而非 chat 内嵌面板或浮窗。

## 3. 元原则：保持精简（keep lean）

三条核心原则是它在三个方向的展开：1 着眼目标用户；2 不试图成为大而全的东西；3 先有理念、再根据理念选择功能。

**原则与理念的分工**：原则是过滤器（回答「要不要做」，说「不」的机制），理念是发生器（回答「整体上是什么、应当长什么样」）。只有原则没有理念，项目只会收缩不会生长——必须有「应该做什么」的正向推导通道。

**精简的对象是表面积，不是代码量、也不是运行时重量**：概念数（状态机分支、例外规则）、依赖数、契约面、文档要解释的特例数。两个反例钉死边界：自写通信层比引 `@opencode-ai/sdk` 代码更多，但契约面更小——精简；D1 选 Electron 比 Tauri 运行时更重——不违反（运行时重量从来不由本原则管辖）。D1–D4 四个技术决策全部符合此读法，本原则是它们共同的隐含理由。

**经济学表述**：功能是收入的增量（一次性），维护是持续的支出；单人项目的维护预算是唯一稀缺资源，**lean = 让表面积匹配维护预算**。

**两个警示边界**：

- lean ≠ 永远最少功能：v1.0 仍须完整到普通人可日常使用（managed 模式属此类）；缺核心能力的「精简」只是半成品
- 文档重 ≠ 不精简：决策记录避免反复重议，是时间维度上的精简——本项目文档密集是 lean 的手段而非反例

## 4. 原则的推导产出（截至 2026-09-04）

> 排期归宿是 spec-v0.4 及以后的版本文件；此处记录推导链（候选 ← 哪条原则），证明这台机器可复用。

| 候选 | 来源 | 初步判断 |
|---|---|---|
| 预览实时刷新（文件变更 → 打开中的预览更新） | 原则一·3 二阶 | 价值高成本低，建议进 v0.4 |
| 未读/新活动标记（Tab 标题/左栏会话行） | 原则一·2 二阶 | 价值高，中等成本 |
| 右键「在文件管理器中显示」（reveal + 选中） | 原则二·移交缺口 | 小成本，v0.4 顺手项 |
| 浏览器工具栏「用系统浏览器打开当前页」 | 原则二·移交缺口 | 小成本 |
| 文件树拖入终端 → 插入转义路径 | 终端·上下文集成层 | 小成本（git add 高频） |
| 标题栏全局 waiting 聚合 | 原则一·2 二阶 | 存疑，观察未读标记效果再议 |
| 会话累计改动总览 | 原则一·1 二阶 | 增量价值待验证；形态已由 EiaT 规定（tab kind） |

**边界 case（判据冲突或未决，出现真实需求再裁）**：

- CSV/表格预览：Chromium 无原生渲染（→ 应外部）vs 渲染成表格技术简单（→ 可自建）——判据冲突时以原则一的频率/重要度裁决
- 「验收通过 = commit」：按原则二应交 git 工具，但验收闭环在应用内完成有价值——未决
- 文件引用的 SymbolSource/ResourceSource（LSP 符号级引用，契约存在、移动端亦未做）——待真实需求
